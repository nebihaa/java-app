pipeline {
    agent any 
    stages('clear') {
    step{
        cheackout scm
        cleanWs()
    }
   }
    stage('build') {
    step{
        def myImage = docker.build (' image-java-app') 
    }
  }
}
