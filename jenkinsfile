pipeline {
    agent any 
    stages('clear') {
    steps{
        cheackout scm
        cleanWs()
    }
   }
    stages('build') {
    steps{
        def myImage = docker.build (' image-java-app') 
    }
  }
}
